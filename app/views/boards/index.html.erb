<h1>Board</h1>

<table class='table'>
  <thead>
    <th>User</th>
    <th>Name</th>

    <th colspan=3>Actions</th>
  </thead>
  <tbody>
    <% @boards.each do |board| %>
      <tr>
        <td><%= board.user_id %></td>
        <td><%= board.name %></td>

        <td><%= link_to "Show", board_path(board) %></td>
        <td></td>
        <td>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>